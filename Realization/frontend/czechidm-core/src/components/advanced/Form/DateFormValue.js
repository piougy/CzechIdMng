import DateTimeFormValue from './DateTimeFormValue';

/**
 * Date form value component
 * - based on DateTimeFormValue - mode (date / datetime) is supported in DateTimeFormValue
 *
 * @author Radek Tomiška
 */
export default class DateFormValue extends DateTimeFormValue {

}
