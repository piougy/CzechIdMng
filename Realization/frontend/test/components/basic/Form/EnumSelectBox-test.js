describe('Form EnumSelectBox', function() {
  it('[check label, if is correctly rendered]');

  it('[check value, if is correctly rendered]');

  it('[get value should return selected item value]');
});
